# Never format outside this repo or into generated / tool-managed trees.
qrusty
requirements-doorstop
# Generated by scripts/traceability.py; prettier's table padding fights the
# generator's output, so leave it as emitted (also excluded from markdownlint).
docs/traceability_report.md
.venv
node_modules
.git
.DS_Store

# Claude Code local state, bind-mounted into the workspace (see .gitignore).
.claude
.claude-home
